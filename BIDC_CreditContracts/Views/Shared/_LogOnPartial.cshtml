@if (Request.IsAuthenticated)
{
    <text>
        Welcome <strong>@User.Identity.Name</strong>! @if (Session["BranchID"] != null)
        {
            <text>| Branch <strong>@Session["BranchID"].ToString()</strong></text>
        }@*
            @*[ @Html.ActionLink("Log Off", "LogOff", "Account") ]*@
    </text>
}
